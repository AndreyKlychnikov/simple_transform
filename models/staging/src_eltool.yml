version: 2

sources:
  - name: postgres
    description: Data from application database, brought in by an EL process.
    tables:
      - name: example_table
        columns:
        - name: id
        - name: value
        - name: ts
